import React from 'react';

const Logo = () => {
    return (
        <nav style={{ display: 'flex', justifyContent: 'flex-end'}}>
            <p>Sign out</p>
        </nav>
    );
}

export default Logo;